import { BaseProfile } from '../types/profile';
import { ASSETS } from './assets';

export const USER_1_BASE_PROFILE: BaseProfile = {
  fullName: 'Robert A.',
  pictureImageSource: ASSETS.images.users['user-1'],
};

export const USER_2_BASE_PROFILE: BaseProfile = {
  fullName: 'Amy S.',
  pictureImageSource: ASSETS.images.users['user-2'],
};

export const USER_3_BASE_PROFILE: BaseProfile = {
  fullName: 'John C.',
  pictureImageSource: ASSETS.images.users['user-3'],
};

export const USER_4_BASE_PROFILE: BaseProfile = {
  fullName: 'Lily H.',
  pictureImageSource: ASSETS.images.users['user-4'],
};
